# Source artwork

This directory contains editable source artwork used to produce the runtime PNG assets. Source editor files are intentionally kept outside every mod resource tree and are excluded from release jars.
