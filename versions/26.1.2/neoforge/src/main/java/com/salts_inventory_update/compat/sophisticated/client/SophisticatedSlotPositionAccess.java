package com.salts_inventory_update.compat.sophisticated.client;

/** Internal mutable view used only while a Sophisticated slot is hosted in a Salt window. */
public interface SophisticatedSlotPositionAccess {
    void salts_inventory_update$setPosition(int x, int y);
}
