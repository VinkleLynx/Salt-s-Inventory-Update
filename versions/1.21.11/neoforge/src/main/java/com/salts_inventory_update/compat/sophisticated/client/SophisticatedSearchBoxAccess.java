package com.salts_inventory_update.compat.sophisticated.client;

/** Internal bridge for the package-private native SearchBox expansion state and hosted bounds. */
public interface SophisticatedSearchBoxAccess {
    boolean salts_inventory_update$isExpandedOrFocused();

    void salts_inventory_update$setHostedBounds(int x, int maximizedWidth);
}
